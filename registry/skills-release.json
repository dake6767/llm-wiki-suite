{
  "schema": 1,
  "pack_version": "2.2.1",
  "pack_notes": "schema-upgrade 不再静默替换手工撰写的 schema：本库自撰内容超过阈值时拒绝执行并要求先看 --diff（升级只搬运 domain 表，其余会被覆盖）。health 也不再把「明确推迟」的 Thesis（`> TBD —— 随研究推进更新`）当成没人填的模板占位符来报。"
}
